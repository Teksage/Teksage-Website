/** Firebase Cloud Messaging — web push (mirrors Flutter `NotificationService`). */

/** Compat SDK loaded in the messaging service worker via `importScripts`. */
export const FIREBASE_SW_COMPAT_VERSION = "12.14.0";

export const FIREBASE_MESSAGING_SW_PATH = "/firebase-messaging-sw.js";

/** System notification icon when a push arrives in the background. */
export const PUSH_NOTIFICATION_ICON = "/flutter-assets/images/teksage-logo.png";
