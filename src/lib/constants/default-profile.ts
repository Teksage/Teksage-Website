import { CHAT_LANGUAGE_OPTIONS } from "@/lib/constants/profile-details";

/** First option in `CHAT_LANGUAGE_OPTIONS` — default profile picklist value. */
export const DEFAULT_CHAT_LANGUAGE = CHAT_LANGUAGE_OPTIONS[0];
