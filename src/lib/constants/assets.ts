/**
 * Public static asset paths (files under /public).
 * Do not hardcode these strings in components — import from here.
 */

export const PUBLIC_ASSETS = {
  /** Login / splash branding (from Flutter `loginLogo.svg`) */
  loginLogo: "/branding/login-logo.svg",
  /** App mark (from Flutter `logo.svg`) */
  appLogo: "/branding/app-logo.svg",
} as const;
