/** Default phone region when UI does not collect country (matches Flutter dev default). */

export const DEFAULT_COUNTRY_CALLING_CODE = "+91" as const;

/** Digits sent to `POST /api/auth/otp/request` as `country_code`. */
export const DEFAULT_COUNTRY_CODE_NUMERIC = "91" as const;
