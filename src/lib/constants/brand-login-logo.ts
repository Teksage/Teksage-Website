/**
 * Login logo dimensions — intrinsic asset is 121×98 (Flutter `loginLogo`).
 * `maxDisplayWidthPx` caps CSS `max-w` in `BrandLoginLogo`.
 */

export const BRAND_LOGIN_LOGO_INTRINSIC_WIDTH = 121 as const;
export const BRAND_LOGIN_LOGO_INTRINSIC_HEIGHT = 98 as const;
export const BRAND_LOGIN_LOGO_DEFAULT_WIDTH_PX = 200 as const;
